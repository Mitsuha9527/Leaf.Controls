<Page x:Class="Leaf.Controls.Gallery.Pages.WindowAndTitleBarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:leaf="http://schemas.Leaf.Controls.com"
      Background="{DynamicResource PrimaryRegionBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <TextBlock FontSize="22" FontWeight="SemiBold" Text="窗口与标题栏" />
            <TextBlock Margin="0,8,0,16" Foreground="{DynamicResource ThirdlyTextBrush}" Text="LeafWindow / TitleBar / TitleBarButton 示例" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="打开 LeafWindow 示例"
                        Margin="0,0,12,0"
                        Click="OnOpenLeafWindowClick" />
                <Button Content="模拟最小化"
                        Click="OnMinimizeMockClick" />
            </StackPanel>

            <Border Padding="16"
                    Background="{DynamicResource RegionBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <DockPanel>
                    <leaf:TitleBar DockPanel.Dock="Top"
                                   ShowHelp="True"
                                   ShowMinimize="True"
                                   ShowMaximize="True"
                                   ShowClose="True" />

                    <StackPanel Margin="0,12,0,0" Orientation="Horizontal">
                        <Button Content="标题按钮 A" Margin="0,0,8,0" />
                        <Button Content="标题按钮 B" />
                    </StackPanel>
                </DockPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
