<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:leaf="clr-namespace:Leaf.Controls.CustomControls"
    xmlns:theme="clr-namespace:Leaf.Controls.Themes">
    <Style x:Key="LabelBaseStyle" TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource TextIconBrush}" />
        <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="leaf:BorderElement.CornerRadius" Value="{x:Static theme:DefaultSettings.DefaultCornerRadius}" />
        <Setter Property="Padding" Value="{x:Static theme:DefaultSettings.DefaultControlPadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="{x:Static theme:DefaultSettings.DefaultMargin}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{x:Static theme:DefaultSettings.FontSize}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Label">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{Binding Path=(leaf:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                        SnapsToDevicePixels="true">
                        <ContentPresenter
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="LabelDefault"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style BasedOn="{StaticResource LabelDefault}" TargetType="Label" />

    <Style
        x:Key="LabelDefault.Small"
        BasedOn="{StaticResource LabelDefault}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="LabelPrimary"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}" />
    </Style>

    <Style
        x:Key="LabelPrimary.Small"
        BasedOn="{StaticResource LabelPrimary}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="LabelSuccess"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource SuccessBrush}" />
    </Style>

    <Style
        x:Key="LabelSuccess.Small"
        BasedOn="{StaticResource LabelSuccess}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="LabelInfo"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource InfoBrush}" />
    </Style>

    <Style
        x:Key="LabelInfo.Small"
        BasedOn="{StaticResource LabelInfo}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="LabelWarning"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource WarningBrush}" />
    </Style>

    <Style
        x:Key="LabelWarning.Small"
        BasedOn="{StaticResource LabelWarning}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

    <Style
        x:Key="LabelDanger"
        BasedOn="{StaticResource LabelBaseStyle}"
        TargetType="Label">
        <Setter Property="Background" Value="{DynamicResource DangerBrush}" />
    </Style>

    <Style
        x:Key="LabelDanger.Small"
        BasedOn="{StaticResource LabelDanger}"
        TargetType="Label">
        <Setter Property="Height" Value="{x:Static theme:DefaultSettings.SmallControlHeight}" />
        <Setter Property="Padding" Value="6,0" />
    </Style>

</ResourceDictionary>