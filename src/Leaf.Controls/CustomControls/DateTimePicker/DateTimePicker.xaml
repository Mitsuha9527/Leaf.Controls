<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:leafui="clr-namespace:Leaf.Controls.CustomControls"
    xmlns:theme="clr-namespace:Leaf.Controls.Themes">

    <Geometry x:Key="CalendarGeometry">M9,10V12H7V10H9M13,10V12H11V10H13M17,10V12H15V10H17M19,3A2,2 0 0,1 21,5V19A2,2 0 0,1 19,21H5C3.89,21 3,20.1 3,19V5A2,2 0 0,1 5,3H6V1H8V3H16V1H18V3H19M19,19V8H5V19H19M9,14V16H7V14H9M13,14V16H11V14H13M17,14V16H15V14H17Z</Geometry>
    <Geometry x:Key="CalendarSelectionGeometry">M19,19H5V8H19M16,1V3H8V1H6V3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3H18V1M17,12H12V17H17V12Z</Geometry>

    <Style x:Key="PathStyle" TargetType="{x:Type Path}">
        <Setter Property="Data" Value="{StaticResource CalendarGeometry}" />
        <Setter Property="Fill" Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="Width" Value="14" />
        <Setter Property="Height" Value="14" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="Margin" Value="0,0,6,0" />
    </Style>

    <Style x:Key="SelectedStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Text" Value="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=SelectedDate, StringFormat={}{0:yyyy-MM-dd}}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="6,0,6,0" />
    </Style>

    <Style x:Key="CalendarSwitch" TargetType="{x:Type ToggleButton}">
        <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="leafui:BorderElement.CornerRadius" Value="{x:Static theme:DefaultSettings.DefaultCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=(leafui:BorderElement.CornerRadius)}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource SelectedStyle}" />
                            <Path
                                x:Name="Path"
                                Grid.Column="1"
                                Style="{StaticResource PathStyle}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Path" Property="Data" Value="{StaticResource CalendarSelectionGeometry}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DayOfWeekLabel" TargetType="{x:Type Label}">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
        <Setter Property="Width" Value="28" />
        <Setter Property="Height" Value="{Binding RelativeSource={RelativeSource Self}, Path=ActualWidth}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Border Background="{TemplateBinding Background}">
                        <TextBlock
                            VerticalAlignment="Center"
                            Text="{TemplateBinding Content}"
                            TextAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  星期日标签样式  -->
    <Style
        x:Key="SundayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Su" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="日" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期一标签样式  -->
    <Style
        x:Key="MondayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Mo" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="一" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期二标签样式  -->
    <Style
        x:Key="TuesdayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Tu" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="二" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期三标签样式  -->
    <Style
        x:Key="WednesdayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="We" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="三" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期四标签样式  -->
    <Style
        x:Key="ThursdayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Th" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="四" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期五标签样式  -->
    <Style
        x:Key="FridayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Fr" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="五" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  星期六标签样式  -->
    <Style
        x:Key="SaturdayLabelStyle"
        BasedOn="{StaticResource DayOfWeekLabel}"
        TargetType="{x:Type Label}">
        <Setter Property="Content" Value="Sa" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=leafui:DateTimePicker}, Path=IsEnglish}" Value="False">
                <Setter Property="Content" Value="六" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Geometry x:Key="ChevronLeftGeometry">M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z</Geometry>
    <Geometry x:Key="ChevronRightGeometry">M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z</Geometry>

    <Style x:Key="ChevronButton" TargetType="{x:Type Button}">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="Margin" Value="10,10,10,5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid Background="{TemplateBinding Background}">
                        <Path
                            x:Name="Path"
                            Width="12"
                            Height="12"
                            Fill="{TemplateBinding Foreground}"
                            Stretch="Uniform" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Tag" Value="Left">
                            <Setter TargetName="Path" Property="Data" Value="{StaticResource ChevronLeftGeometry}" />
                        </Trigger>
                        <Trigger Property="Tag" Value="Right">
                            <Setter TargetName="Path" Property="Data" Value="{StaticResource ChevronRightGeometry}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource MouseMoveBrush}" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="CalendarBox" TargetType="{x:Type ListBox}">
        <Setter Property="SelectedValuePath" Value="DateFormat" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBox}">
                    <ItemsPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="7" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MonthStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Text" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=CurrentMonth, StringFormat={}{0:yyyy.MM}}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="10,10,10,5" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>



    <Style TargetType="{x:Type leafui:DateTimePicker}">
        <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
        <Setter Property="MinWidth" Value="100" />
        <Setter Property="leafui:BorderElement.CornerRadius" Value="5" />
        <Setter Property="Height" Value="30" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type leafui:DateTimePicker}">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding leafui:BorderElement.CornerRadius}">
                        <leafui:SimplePanel>
                            <ToggleButton
                                x:Name="PART_Switch"
                                MinWidth="{TemplateBinding MinWidth}"
                                Style="{StaticResource CalendarSwitch}" />
                            <Popup
                                x:Name="PART_Popup"
                                AllowsTransparency="True"
                                StaysOpen="False"
                                VerticalOffset="2">
                                <Border
                                    Padding="10"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="4">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Button
                                                x:Name="PART_Left"
                                                Style="{StaticResource ChevronButton}"
                                                Tag="Left" />
                                            <TextBlock Grid.Column="1" Style="{StaticResource MonthStyle}" />
                                            <Button
                                                x:Name="PART_Right"
                                                Grid.Column="2"
                                                Style="{StaticResource ChevronButton}"
                                                Tag="Right" />
                                        </Grid>
                                        <UniformGrid Grid.Row="1" Columns="7">
                                            <Label Grid.Column="0" Style="{StaticResource SundayLabelStyle}" />
                                            <Label Grid.Column="1" Style="{StaticResource MondayLabelStyle}" />
                                            <Label Grid.Column="2" Style="{StaticResource TuesdayLabelStyle}" />
                                            <Label Grid.Column="3" Style="{StaticResource WednesdayLabelStyle}" />
                                            <Label Grid.Column="4" Style="{StaticResource ThursdayLabelStyle}" />
                                            <Label Grid.Column="5" Style="{StaticResource FridayLabelStyle}" />
                                            <Label Grid.Column="6" Style="{StaticResource SaturdayLabelStyle}" />
                                        </UniformGrid>
                                        <ListBox
                                            x:Name="PART_ListBox"
                                            Grid.Row="2"
                                            Style="{StaticResource CalendarBox}" />
                                    </Grid>
                                </Border>
                            </Popup>
                        </leafui:SimplePanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
