<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:leafui="clr-namespace:Leaf.Controls.CustomControls"
    xmlns:o="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">

    <Geometry x:Key="WindowMinGeometry" o:Freeze="True">
        M0,4 L10,4 L10,5 L0,5 z
    </Geometry>
    <Geometry x:Key="WindowRestoreGeometry" o:Freeze="True">
        M1,3 L1,9 L7,9 L7,3 z M3,1 L3,2 L8,2 L8,7 L9,7 L9,1 z M2,0 L10,0 L10,8 L8,8 L8,10 L0,10 L0,2 L2,2 z
    </Geometry>
    <Geometry x:Key="WindowMaxGeometry" o:Freeze="True">
        M1,1 L1,9 L9,9 L9,1 z M0,0 L10,0 L10,10 L0,10 z
    </Geometry>
    <Geometry x:Key="CloseGeometry" o:Freeze="True">
        M0.7,0 L5,4.3 L9.3,0 L10,0.7 L5.7,5 L10,9.3 L9.3,10 L5,5.7 L0.7,10 L0,9.3 L4.3,5 L0,0.7 z
    </Geometry>
    <Geometry x:Key="HelpGeometry" o:Freeze="True">
        M20.56,11.57c0-1.19-.24-2.31-.71-3.36-.47-1.05-1.11-1.96-1.92-2.74-.8-.78-1.74-1.4-2.81-1.85-1.07-.45-2.2-.68-3.37-.68s-2.37,.23-3.44,.69c-1.07,.46-2,1.09-2.8,1.88s-1.42,1.73-1.88,2.8c-.46,1.07-.69,2.22-.69,3.44,0,.4-.15,.74-.44,1.03s-.64,.44-1.03,.44c-.49,0-.86-.17-1.1-.52-.25-.34-.37-.74-.37-1.18,0-1.59,.32-3.09,.95-4.49,.63-1.4,1.49-2.62,2.57-3.66,1.08-1.04,2.33-1.86,3.74-2.47,1.42-.6,2.91-.91,4.49-.91s3.07,.3,4.49,.91c1.42,.6,2.66,1.43,3.74,2.47,1.08,1.04,1.93,2.26,2.57,3.66,.63,1.4,.95,2.9,.95,4.49,0,1.24-.13,2.32-.39,3.25-.26,.93-.63,1.77-1.12,2.54-.49,.77-1.08,1.49-1.78,2.18-.7,.69-1.48,1.42-2.35,2.2-.44,.4-.88,.78-1.31,1.16-.43,.38-.84,.78-1.24,1.21-.5,.55-.89,1.08-1.17,1.59-.28,.51-.48,1.03-.62,1.56-.14,.53-.22,1.09-.25,1.69-.03,.6-.05,1.25-.05,1.95,0,.4-.15,.74-.44,1.03-.29,.29-.64,.44-1.03,.44-.34,0-.6-.06-.79-.18-.19-.12-.34-.29-.44-.49-.1-.21-.17-.44-.2-.7-.03-.26-.05-.52-.05-.78v-.78c0-1.21,.15-2.28,.44-3.2,.29-.93,.68-1.76,1.16-2.5,.48-.74,1.03-1.41,1.65-2.02,.62-.6,1.25-1.19,1.89-1.74,.64-.56,1.27-1.11,1.88-1.66,.61-.55,1.16-1.15,1.64-1.8,.48-.65,.87-1.37,1.17-2.16,.3-.79,.45-1.69,.45-2.72Zm-11.02,27.36c0-.61,.21-1.13,.64-1.56,.43-.43,.95-.64,1.56-.64s1.13,.21,1.56,.64c.43,.43,.64,.95,.64,1.56s-.21,1.13-.64,1.56c-.43,.43-.95,.64-1.56,.64s-1.13-.21-1.56-.64c-.43-.43-.64-.95-.64-1.56Z
    </Geometry>

    <Style TargetType="leafui:TitleBarButton">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Command" Value="{Binding Path=TemplateButtonCommand, RelativeSource={RelativeSource AncestorType={x:Type leafui:TitleBar}}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="leafui:TitleBarButton">
                    <Border
                        Margin="{TemplateBinding Margin}"
                        Padding="{TemplateBinding Padding}"
                        VerticalAlignment="Stretch"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="0">
                        <Grid>
                            <Path
                                x:Name="CanvasPath"
                                Width="16"
                                Height="16"
                                VerticalAlignment="Center"
                                Fill="{TemplateBinding Foreground}"
                                Stretch="Uniform" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ButtonType" Value="{x:Static leafui:TitleBarButtonType.Help}">
                            <Setter Property="CommandParameter" Value="{x:Static leafui:TitleBarButtonType.Help}" />
                            <Setter TargetName="CanvasPath" Property="Data" Value="{StaticResource HelpGeometry}" />
                        </Trigger>
                        <Trigger Property="ButtonType" Value="{x:Static leafui:TitleBarButtonType.Minimize}">
                            <Setter Property="CommandParameter" Value="{x:Static leafui:TitleBarButtonType.Minimize}" />
                            <Setter TargetName="CanvasPath" Property="Data" Value="{StaticResource WindowMinGeometry}" />
                        </Trigger>
                        <Trigger Property="ButtonType" Value="{x:Static leafui:TitleBarButtonType.Restore}">
                            <Setter Property="CommandParameter" Value="{x:Static leafui:TitleBarButtonType.Restore}" />
                            <Setter TargetName="CanvasPath" Property="Data" Value="{StaticResource WindowRestoreGeometry}" />
                        </Trigger>
                        <Trigger Property="ButtonType" Value="{x:Static leafui:TitleBarButtonType.Maximize}">
                            <Setter Property="CommandParameter" Value="{x:Static leafui:TitleBarButtonType.Maximize}" />
                            <Setter TargetName="CanvasPath" Property="Data" Value="{StaticResource WindowMaxGeometry}" />
                        </Trigger>
                        <Trigger Property="ButtonType" Value="{x:Static leafui:TitleBarButtonType.Close}">
                            <Setter Property="CommandParameter" Value="{x:Static leafui:TitleBarButtonType.Close}" />
                            <Setter TargetName="CanvasPath" Property="Data" Value="{StaticResource CloseGeometry}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FF0863E3" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF034DB3" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="True" />
                                <Condition Property="ButtonType" Value="Close" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#FFB30000" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="ButtonType" Value="Close" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#AFB30000" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="leafui:TitleBar">
        <Setter Property="Background" Value="{DynamicResource BackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="5,0,0,0" />
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="leafui:TitleBar">
                    <Border
                        x:Name="PART_MainBorder"
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image
                                x:Name="PART_IconImage"
                                Grid.Column="0"
                                Width="{TemplateBinding leafui:IconElement.Width}"
                                Height="{TemplateBinding leafui:IconElement.Height}"
                                Margin="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True"
                                Source="{TemplateBinding leafui:IconElement.DrawingIcon}"
                                Stretch="Fill" />
                            <TextBlock
                                x:Name="PART_Title"
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                FontSize="14"
                                FontWeight="SemiBold"
                                Foreground="{TemplateBinding Foreground}"
                                Text="{TemplateBinding leafui:TitleElement.Title}" />
                            <ContentPresenter Grid.Column="2" Margin="10,0" />
                            <Grid
                                Grid.Column="3"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Top">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border
                                    x:Name="PART_DragAreaBorder"
                                    Grid.Column="0"
                                    Background="Transparent" />
                                <leafui:TitleBarButton
                                    x:Name="PART_HelpButton"
                                    Grid.Column="1"
                                    HorizontalAlignment="Right"
                                    ButtonType="Help" />
                                <leafui:TitleBarButton
                                    x:Name="PART_MinimizeButton"
                                    Grid.Column="2"
                                    HorizontalAlignment="Right"
                                    ButtonType="Minimize" />
                                <leafui:TitleBarButton
                                    x:Name="PART_MaximizeButton"
                                    Grid.Column="3"
                                    HorizontalAlignment="Right"
                                    ButtonType="Maximize" />
                                <leafui:TitleBarButton
                                    x:Name="PART_CloseButton"
                                    Grid.Column="4"
                                    HorizontalAlignment="Right"
                                    ButtonType="Close" />
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="leafui:IconElement.DrawingIcon" Value="{x:Null}">
                            <Setter TargetName="PART_IconImage" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="leafui:TitleElement.Title" Value="{x:Null}">
                            <Setter TargetName="PART_Title" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="ShowMaximize" Value="True" />
                                <Condition Property="IsMaximized" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_MaximizeButton" Property="ButtonType" Value="Restore" />
                        </MultiTrigger>
                        <Trigger Property="ShowHelp" Value="False">
                            <Setter TargetName="PART_HelpButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowMinimize" Value="False">
                            <Setter TargetName="PART_MinimizeButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowMaximize" Value="False">
                            <Setter TargetName="PART_MaximizeButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="ShowClose" Value="False">
                            <Setter TargetName="PART_CloseButton" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>