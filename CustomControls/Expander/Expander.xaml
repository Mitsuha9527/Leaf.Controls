<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:utils="clr-namespace:Leaf.Controls.Utilities"
                    xmlns:leafui="clr-namespace:Leaf.Controls.CustomControls">

    <ResourceDictionary.MergedDictionaries>
        <utils:SharedResourceDictionary Source="/Leaf.Controls;component/Themes/Converters.xaml" />
        <utils:SharedResourceDictionary Source="/Leaf.Controls;component/Resources/GeometryItems/GeometryItems.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="ExpanderDownTemplate" TargetType="Expander">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <ToggleButton x:Name="PART_ToggleButton"
                          Grid.Row="0"
                          Grid.ColumnSpan="2"
                          MinHeight="{TemplateBinding MinHeight}"
                          Margin="0"
                          Padding="5,3"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch"
                          leafui:BorderElement.CornerRadius="{Binding Path=(leafui:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}"
                          Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          Focusable="False"
                          Foreground="{TemplateBinding Foreground}"
                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="30" />
                    </Grid.ColumnDefinitions>
                    <ContentPresenter x:Name="PART_HeaderContent"
                                      Grid.Column="0"
                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                      ContentSource="Header" />
                    <Path Name="PART_PathArrow"
                          Grid.Column="1"
                          Width="12"
                          Margin="5,3"
                          HorizontalAlignment="Right"
                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                          Data="{StaticResource DownSkeletonGeometry}"
                          Fill="{TemplateBinding Foreground}"
                          IsHitTestVisible="False"
                          Stretch="Uniform" />
                </Grid>
            </ToggleButton>
            <Border x:Name="PART_ExpandBorder"
                    Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Padding="{TemplateBinding Padding}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness,
                                                      Converter={StaticResource ThicknessSplitConverter},
                                                      ConverterParameter='1,0,1,1'}"
                    CornerRadius="{Binding Path=(leafui:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource CornerRadiusSplitConverter}, ConverterParameter='0,0,1,1'}"
                    Visibility="Collapsed">
                <ContentPresenter Name="PART_ExpandContent" />
            </Border>

        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="true">
                <Setter TargetName="PART_ToggleButton" Property="leafui:BorderElement.CornerRadius" Value="{Binding Path=(leafui:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource CornerRadiusSplitConverter}, ConverterParameter='1,1,0,0'}" />
                <Setter TargetName="PART_ExpandBorder" Property="Visibility" Value="Visible" />
                <Setter TargetName="PART_PathArrow" Property="Data" Value="{StaticResource UpSkeletonGeometry}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Opacity" Value=".4" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ExpanderBaseStyle" TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        <Setter Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="{DynamicResource RegionBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5,3" />
        <Setter Property="leafui:BorderElement.CornerRadius" Value="5" />
        <Setter Property="Template" Value="{StaticResource ExpanderDownTemplate}" />
    </Style>

    <Style BasedOn="{StaticResource ExpanderBaseStyle}" TargetType="Expander" />
</ResourceDictionary>